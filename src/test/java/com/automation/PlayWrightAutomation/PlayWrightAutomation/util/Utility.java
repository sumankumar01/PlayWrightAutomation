package com.automation.PlayWrightAutomation.PlayWrightAutomation.util;

import org.awaitility.Awaitility;

import java.time.Duration;
import java.util.concurrent.Callable;

public class Utility {

   /* public static void wait(long l, Callable<Boolean> wait)
    {
        Callable<Boolean> wait = () -> page.locator("yourLocator").isVisible(); //you can code your condition here.
        Awaitility.waitAtMost(Duration.ofSeconds(l)).pollDelay(Duration.ofMillis(500)).until(wait);
    }*/
}
